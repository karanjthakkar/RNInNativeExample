import { AppRegistry } from 'react-native';
import App from './src/App';
import Widget from './src/Widget';

AppRegistry.registerComponent('testing', () => App);
AppRegistry.registerComponent('Widget', () => Widget);
